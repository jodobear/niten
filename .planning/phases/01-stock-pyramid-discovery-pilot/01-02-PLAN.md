---
phase: 01-stock-pyramid-discovery-pilot
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [PILOT-02, PILOT-03]
user_setup:
  - service: "Disposable Debian 13 VPS"
    why: "Live stock Pyramid discovery host"
    env_vars:
      - name: NITEN_VPS_HOST
        source: "Protected operator environment"
      - name: NITEN_VPS_USER
        source: "Protected operator environment"
  - service: "DNS authority"
    why: "D-01 public origin"
    dashboard_config:
      - task: "Authorize control for niten.sovereignengineering.io"
        location: "Existing sovereignengineering.io DNS provider"
  - service: "Private operator mesh"
    why: "D-03 private SSH/admin path"
    env_vars:
      - name: NITEN_MESH_IP
        source: "Protected mesh state"
  - service: "Off-host restic repository"
    why: "Recoverable backup outside VPS failure domain"
    env_vars:
      - name: RESTIC_REPOSITORY
        source: "Protected operator environment"
      - name: RESTIC_PASSWORD_FILE
        source: "Protected credential file"
  - service: "Independent external scan runners"
    why: "Complete target-independent IPv4 and IPv6 exposure acceptance"
    env_vars:
      - name: NITEN_SCAN_RUNNER_HANDOFF
        source: "Protected operator handoff describing authorized runners, tools, routes, source identities, and expiry"
must_haves:
  truths:
    - "Operator explicitly selects fresh disposable target, provider/region/AUP, private mesh, DNS control, off-host backup, and recovery access before host mutation."
    - "Protected values exist without being printed or committed."
    - "Selected private path and independent recovery session can survive firewall apply."
    - "Operator authorizes at least one target-independent IPv4 scan vantage and one target-independent IPv6 scan vantage, with full TCP 1-65535 scope, private runner identity, proven family route, and fresh reachability preflight."
  artifacts:
    - path: .planning/phases/01-stock-pyramid-discovery-pilot/01-02-SUMMARY.md
      provides: "Non-secret target-ready and scan-ready selection plus protected input names"
  key_links:
    - from: 01-02-SUMMARY.md
      to: 01-03-PLAN.md
      via: "target-ready checkpoint and protected environment preconditions"
      pattern: "target-ready"
  prohibitions:
    - "No executor-selected provider, region, mesh, DNS, backup, or operator authority."
    - "No hostname, address, credential, token, private CA material, or backup secret enters summary or Git."
---

<objective>
Resolve external trust-boundary facts, including independent external scan authority, before live host work.

Purpose: Prevent guessed infrastructure and unsafe credential handling.
Output: Explicit `target-ready` or `target-not-ready` decision covering target and scan runners; protected values stay outside Git.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-CONTEXT.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-RESEARCH.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize disposable target and private operations plane</name>
  <read_first>
    .planning/STATE.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-CONTEXT.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-RESEARCH.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-01-SUMMARY.md
  </read_first>
  <files>No repository implementation files; summary records only non-secret classes, versions, cost, operator role, UTC, and selected option.</files>
  <decision>Whether disposable Debian 13 VPS, provider/region/AUP, DNS authority, approved private mesh path, independent recovery session, off-host restic target, authorized target-independent IPv4/IPv6 scan runners, and accountable operator are ready.</decision>
  <options>
    <option id="target-ready">
      <name>Target ready</name>
      <pros>All external trust-boundary facts, protected inputs, and external scan authorities are ready.</pros>
      <cons>Operator accepts provider, mesh, DNS, backup, cost, recovery, and scan responsibility.</cons>
    </option>
    <option id="target-not-ready">
      <name>Target not ready</name>
      <pros>No infrastructure choice or protected value is guessed.</pros>
      <cons>Live deployment remains blocked until missing inputs exist.</cons>
    </option>
  </options>
  <instructions>Choose `target-ready` only after protected variables exist, target is disposable, backup is outside VPS/provider failure domain, operator device is mesh-enrolled, DNS scope is authorized, and independent recovery path is available. Also select and authorize at least one target-independent IPv4 runner and one target-independent IPv6 runner for TCP 1-65535; one off-host runner may cover both only when its IPv4 and IPv6 routes are each proven independent from the target. Privately retain runner/source identity, source IP, tools/versions, scan authority, route-family proof, reachability preflight, approval UTC, and expiry in `NITEN_SCAN_RUNNER_HANDOFF`; never put these values in Git or summary. Record provider class, region, Debian image identifier, mesh product/version, backup class/region, monthly cost, operator role, scan-ready status, and UTC without live endpoint, source identity, or credential values. Choose `target-not-ready` and list missing input names otherwise.</instructions>
  <verification>
    <automated>test -n "${NITEN_VPS_HOST:-}" &amp;&amp; test -n "${NITEN_VPS_USER:-}" &amp;&amp; test -n "${NITEN_MESH_IP:-}" &amp;&amp; test -n "${RESTIC_REPOSITORY:-}" &amp;&amp; test -r "${RESTIC_PASSWORD_FILE:-/nonexistent}" &amp;&amp; test -r "${NITEN_SCAN_RUNNER_HANDOFF:-/nonexistent}" &amp;&amp; ./scripts/repo-preflight.sh --tracked --staged</automated>
    <human-check>Confirm authority, cost/AUP/region, disposable status, private access, recovery session, independent backup failure domain, and fresh authorized target-independent IPv4/IPv6 runners with successful family reachability preflights and full-port scope.</human-check>
  </verification>
  <acceptance_criteria>`target-ready` and `scan-ready` are explicit, all protected preconditions pass, and both family runners are authorized/reachable/fresh, or execution stops without host changes.</acceptance_criteria>
  <resume-signal>Select `target-ready` or `target-not-ready` with missing input names.</resume-signal>
</task>

</tasks>

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks dependent live action.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator authority → provider/DNS/mesh/backup | External accounts define jurisdiction, reachability, recovery, and cost. |
| Protected environment → executor | Endpoint and credential values must be usable without disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Spoofing | provider/DNS/mesh authority | high | mitigate | Human authorization checkpoint, named accountable role, protected-variable assertions, no guessed target. |
| T-02-02 | Information Disclosure | protected inputs | high | mitigate | Value-free checks and summaries; repository preflight; no endpoint/credential output. |
| T-02-03 | Denial of Service | firewall recovery path | high | mitigate | Require validated private path plus independent recovery session before host apply. |
| T-02-04 | Repudiation | target selection | low | accept | Non-secret option, rationale, operator role, and UTC retained; account identifiers remain private. |
| T-02-05 | Spoofing / Repudiation | external scan runners | high | mitigate | Human-authorized target-independent IPv4/IPv6 runners, private source identity/tool/route evidence, explicit TCP 1-65535 authority, preflight, and expiry. |
</threat_model>

## Artifacts this phase produces

- `01-02-SUMMARY.md` with non-secret selection only.
- Protected execution environment with target, mesh, DNS, backup, and external runner handoff values outside Git.

<verification>
Checkpoint must resolve `target-ready` and `scan-ready`; all value-presence, family preflight, authority, freshness, and repository-preflight checks pass without printing values.
</verification>

<success_criteria>
- No live target fact is invented.
- Negative selection stops before host mutation.
</success_criteria>

<output>
Create `.planning/phases/01-stock-pyramid-discovery-pilot/01-02-SUMMARY.md` when done.
</output>
