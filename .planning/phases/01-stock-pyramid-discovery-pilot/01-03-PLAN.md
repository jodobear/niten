---
phase: 01-stock-pyramid-discovery-pilot
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - deploy/systemd/pyramid.service
  - deploy/systemd/niten-journal.conf
  - deploy/settings/settings.example.json
  - scripts/deploy-private-host.sh
  - docs/effective-settings-review.md
  - deploy/caddy/Caddyfile
  - deploy/nftables/niten.nft
  - scripts/verify-effective-settings.sh
  - docs/operations.md
autonomous: true
requirements: [PILOT-01, PILOT-02, PILOT-03]
must_haves:
  truths:
    - "Exact verified source-unmodified Pyramid runs as dedicated user on 127.0.0.1:3334 with 0700 state, UMask 0077, 0600 secret-bearing files, strict writable paths, bounded logs, and no automatic updates."
    - "Caddy is the only intended public listener; private admin origin binds approved mesh path and public privileged routes are denied before reverse proxy."
    - "Complete host/provider/DNS IPv4/IPv6 inventory gates lockout-safe nftables apply; public TCP permits only 80/443 while SSH/admin remain private."
    - "Effective live settings yield redacted report, canonical hash, exact template comparison, operator review, and opaque private-journal pointer; live settings remain private."
    - "Installed target unit passes `systemd-analyze verify` and has exposure score 4.0 or lower with no unresolved phase-reviewed high finding; raw assessment stays private and redacted score/summary is reviewed before public or cohort work."
    - "D-01/D-02/D-03/D-05: named public origin permits anonymous explicitly-public reads and allowlisted publishing while all administration remains private under accurate experimental metadata."
  artifacts:
    - path: deploy/systemd/pyramid.service
      provides: "Least-privilege loopback stock Pyramid service"
    - path: deploy/caddy/Caddyfile
      provides: "Private admin/public protocol route boundary"
    - path: deploy/nftables/niten.nft
      provides: "Dual-stack host ingress policy with failsafe deployment"
    - path: docs/effective-settings-review.md
      provides: "Concise redacted effective-settings review format"
  key_links:
    - from: deploy/caddy/Caddyfile
      to: 127.0.0.1:3334
      via: "deny-first ordinary HTTPS/WSS reverse proxy"
      pattern: "reverse_proxy 127.0.0.1:3334"
    - from: scripts/deploy-private-host.sh
      to: deploy/nftables/niten.nft
      via: "inventory reconciliation, syntax check, timed rollback, apply, confirm, reboot"
      pattern: "nft"
    - from: scripts/verify-effective-settings.sh
      to: deploy/settings/settings.example.json
      via: "protected live comparison and redacted hash/review output"
      pattern: "settings.example.json"
  prohibitions:
    - "No public Pyramid/admin/SSH listener, provider-firewall-only control, or public admin cookie boundary."
    - "No live settings, address, interface, DNS answer, mesh detail, CA private key, prior/live ruleset, cookie, or credential enters Git."
---

<objective>
Deploy exact stock Pyramid private-first under least privilege, dual-stack firewall, private administration, and reviewed effective settings.

Purpose: Build minimum safe live-test host without opening public ingress.
Output: Target-valid systemd/settings/Caddy/nftables files, safe deploy/verify scripts, private admin trust, operations runbook, and redacted effective-settings review.
</objective>

<execution_context>
@/home/at/.codex/gsd-core/workflows/execute-plan.md
@/home/at/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-CONTEXT.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-RESEARCH.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-PATTERNS.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-01-SUMMARY.md
@.planning/phases/01-stock-pyramid-discovery-pilot/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install exact stock Pyramid as private least-privilege service</name>
  <precondition>Plan 01-02 selected `target-ready`; protected target values exist; private SSH and independent recovery session both reach fresh Debian 13 host.</precondition>
  <read_first>
    config/pyramid.lock
    scripts/verify-pyramid.sh
    .planning/phases/01-stock-pyramid-discovery-pilot/01-RESEARCH.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-PATTERNS.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-02-SUMMARY.md
  </read_first>
  <files>deploy/systemd/pyramid.service, deploy/systemd/niten-journal.conf, deploy/settings/settings.example.json, scripts/deploy-private-host.sh, docs/effective-settings-review.md</files>
  <action>Append target preflight to private journal before host command. Re-run selected pin verification; halt and return to Plan 01-01 if upstream selection changes. Install exact verified artifact under immutable `/opt/niten/pyramid/releases/<tag>-<commit>` and atomically select `current`. Record exact Debian/systemd/Caddy/restic versions and canonical install checksums privately. Create dedicated `pyramid` user, `0700` generation paths, protected environment/settings, `UMask=0077`, no capabilities, strict filesystem/home/kernel protections, explicit `/var/lib/pyramid` write path, `HOST=127.0.0.1`, port 3334, no auto-update, bounded restart, and no SFTP. Set journald namespace to 1 GiB persistent, 256 MiB runtime, 14-day maximum, and 1000 messages/30 seconds. Enable groups/search required by pilot; set D-05 name/contact/rules/experimental/operator-readable wording; disable invitations/access requests/anonymous ephemeral/scheduled behavior plus unrelated internal/personal/favorites/inbox/bookmarks/popular/uppermost/moderated/Blossom/GRASP/nsite/stream/imgproxy/link-preview/paywall/operator/LiveKit modules. Start privately, assert exact socket/modes/service hardening, and run private NIP-11/WSS smoke. Do not enable public DNS/ingress.

After rendering and installing `/etc/systemd/system/pyramid.service`, run target-side `systemd-analyze verify /etc/systemd/system/pyramid.service` and `systemd-analyze security pyramid.service`. Retain raw output privately. Phase acceptance requires exposure score `&lt;= 4.0` and no unresolved finding rated high by the phase threat review. Record only redacted score/summary, reviewer, UTC, and opaque pointer in `docs/effective-settings-review.md`. Any exception records owner, reason, and expiry; an exception cannot unblock public/cohort work while a high finding remains unresolved.

Create `docs/effective-settings-review.md` as concise fillable report with selected pin, template hash, canonical protected-live hash, exact comparison status, disabled-module review, systemd exposure score/redacted finding summary/exception fields, secret rejection, operator role/UTC, and opaque private-journal pointer. No key/value dump or private endpoint. Populate safe fields only after `scripts/deploy-private-host.sh` reads live settings through protected target-side channel and records full comparison and raw systemd assessment privately.</action>
  <verify>
    <automated>shellcheck scripts/deploy-private-host.sh &amp;&amp; systemd-analyze verify deploy/systemd/pyramid.service &amp;&amp; ./scripts/deploy-private-host.sh --self-test-service &amp;&amp; ./scripts/deploy-private-host.sh --verify-private &amp;&amp; ./scripts/deploy-private-host.sh --verify-systemd-security --max-exposure 4.0</automated>
    <human-check>Review private target transcript: exact pin, loopback socket, service hardening, 0700/0600 modes, log limits, minimum enabled modules, explicit disabled modules, installed-target verify/security raw output, exposure score at most 4.0, no unresolved high finding, and no public ingress. Confirm every lower-risk exception has owner/reason/expiry.</human-check>
  </verify>
  <acceptance_criteria>Exact stock service survives restart/reboot privately; only loopback Pyramid socket exists; file modes and log bounds match; installed target unit verifies with exposure score at most 4.0 and no unresolved high finding; effective-settings review has redacted score/hashes/status/operator/journal pointer without live values.</acceptance_criteria>
  <done>Operator can use exact private stock Pyramid under least privilege; public remains closed.</done>
</task>

<task type="auto">
  <name>Task 2: Establish lockout-safe public/private network boundary and private admin trust</name>
  <precondition>Task 1 private Pyramid health passes; two independent private recovery sessions remain open; public DNS/ingress is disabled.</precondition>
  <read_first>
    deploy/systemd/pyramid.service
    deploy/settings/settings.example.json
    docs/effective-settings-review.md
    .planning/phases/01-stock-pyramid-discovery-pilot/01-RESEARCH.md
  </read_first>
  <files>deploy/caddy/Caddyfile, deploy/nftables/niten.nft, scripts/verify-effective-settings.sh, docs/operations.md</files>
  <action>Generate exact public privileged-route list from selected Pyramid source, including setup/settings/clients/database/event/log/reindex/update/restart/action/sync and every enabled-or-disabled module route. In `deploy/caddy/Caddyfile`, deny privileged routes and public management POST with fixed empty 404 before proxy; forward ordinary stock public/NIP-11/WSS routes to loopback; bind private admin origin only to approved mesh address with Caddy internal TLS; rotate Caddy logs at 100 MiB with five files. Export only internal public root certificate to operator-selected `0600` path outside repository, compare independent host/device SHA-256 fingerprints out of band, and install trust only in isolated pilot OS/browser profile. Never export CA private key. Document trust removal/rotation and prove retired chain fails.

Before firewall render, capture protected host kernel/provider inventory of every routable interface, assigned/routed public IPv4/IPv6 address including secondary/temporary addresses, and every current A/AAAA answer for public/admin names. Normalize and require exact host/provider/DNS mapping; separately verify private interface/address exists and is not public. Empty, extra, stale, unresolved, ambiguous, family-incomplete, or changed sets block apply. Render `deploy/nftables/niten.nft` as full `inet` policy: preserve loopback/established traffic; default-deny unsolicited input and forwarding; permit public TCP 80/443 only on reconciled intended public addresses; deny public 22/admin on all public addresses; permit SSH/admin only when private interface and destination match. Provider firewall remains defense in depth. Keep rendered live sets privately. Syntax-check complete policy, save prior rules privately, arm bounded timed rollback, apply with two sessions, verify both families/paths, re-inventory, cancel rollback only after success, persist, reboot, and repeat. `scripts/verify-effective-settings.sh` compares every expected non-secret value/disabled override against template through protected input, writes only safe hashes/status/review/pointer to `docs/effective-settings-review.md`, and fails if output contains settings values or protected runtime facts. `docs/operations.md` records ordered private bootstrap, firewall failsafe, service lifecycle, route review, internal CA removal/rotation, and public-cutover prerequisite commands.</action>
  <verify>
    <automated>shellcheck scripts/verify-effective-settings.sh scripts/deploy-private-host.sh &amp;&amp; caddy validate --config deploy/caddy/Caddyfile --adapter caddyfile &amp;&amp; ./scripts/deploy-private-host.sh --check-network-inventory &amp;&amp; ./scripts/deploy-private-host.sh --check-firewall &amp;&amp; ./scripts/deploy-private-host.sh --verify-after-reboot &amp;&amp; ./scripts/verify-effective-settings.sh --review docs/effective-settings-review.md &amp;&amp; ./scripts/repo-preflight.sh --tracked --staged</automated>
    <human-check>Privately inspect full dual-stack inventory and firewall apply/rollback/reboot transcript; confirm public 22/admin denied, private SSH/admin retained, internal CA fingerprints matched, old trust removal works, public ingress remains disabled, and effective settings match reviewed template.</human-check>
  </verify>
  <acceptance_criteria>Both-family inventory reconciles before/after apply and reboot; timed rollback is proven; private admin works only over mesh; public privileged routes deny; redacted effective-settings report/hash/operator review/private pointer passes.</acceptance_criteria>
  <done>Private-first host boundary is safe and recoverable enough to begin recovery drills, with public ingress still gated.</done>
</task>

</tasks>

<threat_model>
ASVS level: L1. Any unresolved high-severity threat blocks dependent live action.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Internet/private mesh → nftables/Caddy | Public and approved operator traffic cross host boundary. |
| Caddy → loopback Pyramid | One source-unmodified backend serves public and private origins. |
| Operator browser → stock admin/CDN/session | Long-lived JavaScript-readable cookie and third-party resources affect privileged authority. |
| Pyramid → settings/state/logs | Relay secret, management state, events, and logs cross host storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Elevation of Privilege | public admin/routes | high | mitigate | Pin-derived route inventory, deny-before-proxy, fixed 404, public management denial, private-origin binding. |
| T-03-02 | Spoofing | admin cookie/CDN/private TLS | high | mitigate | Mesh-only origin, isolated pilot identity/profile, out-of-band public-root fingerprint, trust rotation/removal, no valuable signer. |
| T-03-03 | Information Disclosure | settings/state/logs | high | mitigate | Dedicated user, 0700/0600, UMask 0077, strict paths, bounded logs, protected settings comparison. |
| T-03-04 | Tampering | Pyramid/Caddy artifacts | high | mitigate | Canonical exact checksums, immutable releases, revalidation, no automatic update. |
| T-03-05 | Denial of Service | remote firewall apply | high | mitigate | Two sessions, complete policy syntax check, timed rollback, both-family verification, persistence only after reboot. |
| T-03-06 | Elevation of Privilege | forgotten IPv6/secondary address | high | mitigate | Host/provider/A+AAAA inventory reconciliation before and after apply/reboot; unresolved mismatch blocks. |
| T-03-07 | Information Disclosure | effective settings/report | high | mitigate | Protected target-side input, safe hashes/status only, operator review, private pointer, repository preflight. |
| T-03-08 | Elevation of Privilege | installed systemd sandbox | high | mitigate | Target-side verify/security assessment, exposure threshold 4.0, phase threat review, private raw output, redacted summary, and blocking unresolved-high rule. |
</threat_model>

## Artifacts this phase produces

- Deployment templates/scripts/runbooks listed in frontmatter.
- Private host inventory, rendered rules, CA fingerprints, settings comparison, and command captures outside Git.

<verification>
Run both task gates. Inspect exact pin, installed-target systemd verify/security assessment and threshold, route inventory, firewall failsafe, reboot, CA trust lifecycle, effective-settings review, and repository preflight.
</verification>

<success_criteria>
- Exact stock Pyramid is loopback-only and least privilege.
- Installed service exposure score is at most 4.0 with no unresolved high threat-review finding; exceptions have owner/reason/expiry.
- Private admin boundary works; public privileged surface is denied.
- Dual-stack firewall is inventory-complete and rollback-safe.
- Live settings remain private while redacted hashes/status/review are retained.
</success_criteria>

<output>
Create `.planning/phases/01-stock-pyramid-discovery-pilot/01-03-SUMMARY.md` when done.
</output>
